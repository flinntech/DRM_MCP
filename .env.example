# Digi Remote Manager MCP Server Environment Variables

# ============================================
# REQUIRED: API Key Authentication
# ============================================
# Get your API key from: https://remotemanager.digi.com
# Navigate to: Profile → API Keys → Create New API Key
# 
# SECURITY: These credentials provide full access to your DRM account.
# Keep them secure and never commit them to version control.

DRM_API_KEY_ID=your_api_key_id_here
DRM_API_KEY_SECRET=your_api_key_secret_here

# ============================================
# OPTIONAL: Server Configuration
# ============================================

# Node environment
NODE_ENV=production

# MCP Transport Type
# - stdio: For Claude Desktop and similar clients (default)
# - http: For n8n, custom integrations, or HTTP-based clients
MCP_TRANSPORT=stdio

# HTTP Server Port (only used when MCP_TRANSPORT=http)
MCP_PORT=3000

# ============================================
# NOTES
# ============================================
# 
# For stdio mode (Claude Desktop):
#   Just set DRM_API_KEY_ID and DRM_API_KEY_SECRET
#   The server will communicate via standard input/output
#
# For HTTP mode (n8n, custom clients):
#   Set MCP_TRANSPORT=http and optionally MCP_PORT
#   Access the server at: http://localhost:3000/mcp
#   Health check at: http://localhost:3000/health
#
# API Key Security:
#   - API keys have the same permissions as your user account
#   - You can create multiple keys with different permissions
#   - Rotate keys regularly for security
#   - Delete unused keys from the DRM console
#
# Query Language Examples:
#   connection_status = "connected"
#   last_update > -1d
#   tags = 'sensor' and connection_status = 'connected'
#   group startsWith '/production/'
#
# Time Formats:
#   Relative: -1h, -1d, -7d, -30d
#   Absolute: ISO 8601 format (2024-01-15T10:30:00Z)
