# Digi Remote Manager MCP Server Environment Variables

# RECOMMENDED: API Key Authentication
# Get your API key from: https://remotemanager.digi.com (Profile → API Keys)
# You need both the API Key ID and the API Key Secret
DRM_API_KEY_ID=your_api_key_id_here
DRM_API_KEY_SECRET=your_api_key_secret_here

# MCP Server Transport Configuration (optional)
# MCP_TRANSPORT=stdio  # Default: stdio (for CLI usage)
# MCP_TRANSPORT=http   # Use HTTP for web integrations (n8n, etc.)
# MCP_PORT=3001        # Port for HTTP transport (default: 3001)

# Node Environment (optional)
NODE_ENV=production
